let obj = {
    roll_no : 1,
    name : "Raj",
}

console.log(`Student Roll Number is ${obj.roll_no} and Name is ${obj.name}.`)

let fname = "Darshan";
let lname = "Virani";

console.log(`First Name is ${fname} and Last Name is ${lname}.`);